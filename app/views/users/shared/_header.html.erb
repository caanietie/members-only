<nav class="navbar navbar-default navbar-fixed-top blog-masthead">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand navbar-link" href="#">
        <%= image_tag "logo.png", alt: "Brand Image", size: "60x40", style: "transform:translateY(-20%)" %>
      </a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      <% if params[:controller].eql? "posts" and params[:action].eql? "index" %>
        <li class = "active">
      <% else %>
        <li>
      <% end %>
          <a href="<%= posts_path %>" class = "navbar-link">
            <span class = "glyphicon glyphicon-home" aria-hidden="true"></span> Home
          </a>
        </li>
      <% if params[:controller].eql? "contacts" %>
        <li class = "active">
      <% else %>
        <li>
      <% end %>
          <a href="<%= new_contact_path %>" class = "navbar-link">
            <span class = "glyphicon glyphicon-envelope" aria-hidden="true"></span> Contact
          </a>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <% handler = user_path(current_user.username)+"?p=1" %>
            <a href="<%=handler%>" class= "navbar-link">
              <span class = "glyphicon glyphicon-user" aria-hidden="true"></span>
              <%= current_user.username %>
            </a>
          </li>
          <li>
            <a href="<%=new_post_path%>" class="navbar-link">
              <span class = "glyphicon glyphicon-pencil" aria-hidden="true"></span> New Post
            </a>
          </li>
          <li>
            <a href="<%=destroy_user_session_path%>" class="btn btn-default" role="button" data-turbo-method="delete">
              <span class = "glyphicon glyphicon-log-out" aria-hidden="true"></span> Sign Out
            </a>
          </li>
        <% else %>
          <li>
            <a href="<%=new_user_session_path%>" class="navbar-link">
              <span class = "glyphicon glyphicon-log-in" aria-hidden="true"></span> Sign In
            </a>
          </li>
          <li>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-default", role: :button %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
